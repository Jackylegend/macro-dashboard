# Copy this to .env and fill in your actual values
# PostgreSQL connection (read-only — no API keys needed here)
PGHOST=your-server.postgres.database.azure.com
PGUSER=your_username
PGPASSWORD=your_password
PGPORT=5432
PGDATABASE=postgres
